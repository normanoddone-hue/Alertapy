{
  "name": "alertapy-web",
  "version": "0.1.0",
  "private": true,
  "description": "AlertaPy — sitio + piloto real de alertas de calor/asma por WhatsApp para San Lorenzo, Paraguay",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check-now": "node src/scheduler.js --once"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
